import { TaskOwnerPixPayment } from './TaskOwnerPixPayment';

describe('TaskOwnerPixPayment Component', () => {
  it('should export TaskOwnerPixPayment component', () => {
    expect(TaskOwnerPixPayment).toBeDefined();
    expect(typeof TaskOwnerPixPayment).toBe('function');
  });
});
