import FeatureDetailPage from "./page";

describe("FeatureDetailPage Component (#68)", () => {
  it("should export FeatureDetailPage default component function", () => {
    expect(typeof FeatureDetailPage).toBe("function");
  });
});
